# MENPS Frontend — Environment Configuration
# Copy this file to `.env` and adjust as needed.

# Base URL of the MENPS FastAPI backend (see backend/.env.example)
VITE_API_BASE_URL=http://localhost:8000
